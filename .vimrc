set shiftwidth=4     " use indents of 3 spaces
set tabstop=4        " an indendation every 4 columns
set expandtab        " tabs are spaces, not tabs

set textwidth=80     " keep lines at an 80 char max for good practice
set wrap             " wrap to line below when overfilling
set number           " show line numbers on left
